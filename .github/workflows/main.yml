
name: test push tag

on:
  push:
    branches:
      - master
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
      # - name: Clone Repository
      #   uses: actions/checkout@v2
      - name: push tag
        if: github.event.ref_type == 'tag'
        run: |
          ls
